import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    root: {
        flex: 1,
        display: 'flex',
        justifyContent: 'flex-end',
        alignItems: 'center',
        padding: '10%',
    },
});
